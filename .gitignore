# Hugo build output and caches
/public/
/resources/_gen/
/.hugo_build.lock
hugo_stats.json

# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Editor / OS noise
.DS_Store
*.swp
